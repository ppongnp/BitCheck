#include "summaries.h"



summaries::summaries(QString avgs, QString closed, QString highs, QString lows, QString opens, QString vol)
{
    avg = avgs;
    close = closed;
    high = highs;
    low = lows;
    open = opens;
    volume = vol;
}
